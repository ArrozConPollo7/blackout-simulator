name = "energia-en-crisis-api"
main = "src/index.ts"
compatibility_date = "2026-09-24"
compatibility_flags = ["nodejs_compat"]

# La URL del proyecto es publica (tambien viaja al navegador), no es un secreto.
routes = [{ pattern = "api.elpollitopio.me", custom_domain = true }]
      [vars] 
SUPABASE_URL = "https://tbwtphcydwhpeqmdfmlo.supabase.co"
ALLOWED_ORIGINS = "https://juego.elpollitopio.me"

# Secretos (nunca en este archivo):
#   wrangler secret put SUPABASE_SERVICE_ROLE_KEY
#   wrangler secret put HOST_TOKEN
